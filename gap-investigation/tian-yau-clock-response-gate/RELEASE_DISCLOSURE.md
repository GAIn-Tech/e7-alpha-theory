# Release disclosure

This lane derives the **leading weak-coupling/large-radius heterotic standard-embedding** canonical dilaton/volume response for the existing Tian–Yau Q and extends it to a conditional electromagnetic matching and two-clock Jacobian. It does not prove a realistic photon sector, stabilized vacuum, time evolution, numerical alpha or clock signal. E6 replaces E7 in this actual compactification.

External inputs: sourced CY geometry and tangent embedding; standard supergravity metric/action and heterotic gauge-threshold formulas; rounded historical atomic sensitivities. Analytic derivations: Weyl weight cancellation, homogeneity pullback, canonical coordinates, actual fixed-topology family. Executed exact checks: symbolic differentiation and kernels. Executed SMT: conditional polynomial consequences, identifiability and counterfamilies. Executed Lean: projection identification and obstruction to full inversion, with explicit finite witness, no project axioms. These layers must not be conflated.

The model-dependent absolute intercept and physical threshold-corrected response remain undetermined. The universal pure-dilaton tree fractional gradient is nevertheless fixed by the action, **not the index**, and this positive result must not be erased by saying everything is free. Same-topology backgrounds change the intercept while retaining that universal tree gradient.

The report preserves source sign inconsistency for Al/Hg, loop-induced linear/chiral dilaton distinctions, unspecified nuisance mass/nuclear responses, and the absence of any scalar history. SAT assignments are mathematical witnesses, never observed data. A complete numerical threshold calculation, corrected kinetic metric and stabilized vacuum could change the conclusion's premises; no all-orders no-go is claimed.

Only `gap-investigation/tian-yau-clock-response-gate` is owned/written. Read-only reuse extracts the predecessor SVD null function by AST, so no sibling module initialization or bytecode writing occurs. Native paths were restored from tool-created Cygwin shadows before executing. HTTP source bytes have no TLS guarantee; their hashes match the previous independent capture. HTTPS extraction failed. Initial Lean-output parser expected a different axiom-report wording and was corrected after inspecting real exit-zero kernel output; no proof was weakened.

AI-assisted, self-assessed; no hosted CI, independent expert review, publication, commit or push. Reproduce locally with native Python 3.13 (SymPy and NumPy) plus the existing gap-investigation Z3 and Lean installations:

```
C:/Python313/python.exe -B C:/Users/mikeb/e7-alpha-theory/gap-investigation/tian-yau-clock-response-gate/calculate.py --verify
C:/Python313/python.exe -B C:/Users/mikeb/e7-alpha-theory/gap-investigation/tian-yau-clock-response-gate/calculate.py --verify --negative-controls
```

`certificate.json` must already exist. `--create` is an explicit exclusive certificate creation path; it never overwrites the certificate. The replay checks hashes and literal equation excerpts. Source acquisition/preparation is separate from replay and is not needed to verify retained evidence.
